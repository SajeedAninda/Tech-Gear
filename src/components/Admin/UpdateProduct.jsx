import React from 'react';

const UpdateProduct = ({params}) => {
    // console.log(params.id)
    return (
        <div>
            <h1>{params.id}</h1>
        </div>
    );
};

export default UpdateProduct;