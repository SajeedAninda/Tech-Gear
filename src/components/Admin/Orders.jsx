import React from 'react';
import useAxiosInstance from '../Hooks/useAxiosInstance';

const Orders = () => {
    

    return (
        <div>
            
        </div>
    );
};

export default Orders;